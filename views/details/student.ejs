<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Details Form</title>
    <style>
        * {
            margin: 0px;
            padding: 0px;
        }
        body {
            font-family: 'Arial', sans-serif;
            background-color: #2c3e50; /* Dark background */
            color: #e0e0e0; /* Light text color */
            margin: 0;
            padding: 15px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .form-container {
            background-color: #10121a; /* Darker background for form container */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 500px;
            color: #e0e0e0;
        }

        .form-container h2 {
            margin-bottom: 20px;
            color: #e0e0e0;
            text-align: center;
            font-size: 24px;
        }

        .form-container label {
            display: block;
            margin-bottom: 5px;
            color: #e0e0e0;
            font-size: smaller;
        }

        .form-container input[type="text"],
        .form-container input[type="email"],
        .form-container select {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #3b4e63; /* Subtle border color */
            border-radius: 5px;
            background-color: #2c3e50; /* Dark input background */
            color: #e0e0e0; /* Light text color in inputs */
            box-sizing: border-box;
        }

        .form-container button {
            width: 100%;
            padding: 10px;
            background-color: #1abc9c; /* Button color */
            border: none;
            border-radius: 5px;
            color: #ffffff; /* Button text color */
            font-size: 13px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .form-container button:hover {
            background-color: #16a085; /* Darker button color on hover */
        }

        .form-container img {
            max-width: 20%;
            max-height: 20%;
            object-fit: contain; /* Scales image while preserving aspect ratio */
            display: block;
            margin: 0 auto 20px auto; /* Center image and add margin at the bottom */
        }

        .field {
            margin-bottom: 15px;
            padding: 10px;
            background-color: #2c3e50; /* Dark background for input containers */
            border-radius: 5px;
            border: 1px solid #3b4e63; /* Subtle border color */
        }
    </style>
</head>
<body>

<div class="form-container">
    <img src="../../form.png" alt="Form Image">
    <h2>Student Details</h2>
    <form action="/Student/save_details" method="POST">
        <div class="field">
            <label for="studentName">Student Name:</label>
            <input type="text" id="studentName" name="studentName" value="<%= studentName %>" required readonly>
        </div>

        <div class="field">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" required>
        </div>

        <div class="field">
            <label for="currentLevelOfEducation">current Level of Education:</label>
            <select id="currentLevelOfEducation" name="currentLevelOfEducation" required>
                <option value="Primary school">primary school</option>
                <option value="High School">High School</option>
                <option value="Associate Degree">Associate Degree</option>
                <option value="Bachelor's Degree">Bachelor's Degree</option>
                <option value="Master's Degree">Master's Degree</option>
                
            </select>
        </div>

        <div class="field">
            <label for="instituteName">Institute Name:</label>
            <input type="text" id="instituteName" name="instituteName">
        </div>

        <button type="submit">Submit</button>
    </form>
</div>

</body>
</html>
